# Context

## Glossary

- `Owner`: the single operator who approves permissions and manages skills for this Felix instance.
- `Session`: a persisted interaction tied to one source thread and one Codex run history.
- `Session record`: the on-disk durable representation of a `Session`, stored under `workspace/sessions/<source>/`.
- `Thread key`: an opaque stable source-thread identifier produced by a source adapter; core modules store and index it but do not parse it.
- `Thread query`: read-only lookups over the session store. Thread lookup by key is `findThreadHandle` in `src/slices/sessions/index.ts` (source-scoped index lookup) — neither the engine nor the owner console (`loadSessionDetail`) re-lists all handles to find one. Pending approval lookup is not a Thread query; it is owned by the Approval registry because it exists solely to resolve live approval requests.
- `Session transitions`: the named, atomic mutations of session state in `src/slices/sessions/index.ts` — queue movement (`queueThreadEvent`, `shiftNextEvent`, `requeueEvent`, `clearThreadQueue`, `filterThreadQueue`), harness state (`recordTurn`, `clearHarnessSession`, `setThreadBusy`), pending-permission compatibility (`setPendingPermission`), and usage snapshot state (`recordTurnUsage`). Each owns one load-mutate-save; the engine expresses intent through them rather than hand-writing load/mutate/save.
- `Owner read facade`: the owner-console aggregation layer in `src/owner-data.ts`. It composes reads across the `sessions`, `events`, `approvals`, `audit`, and `usage` Modules into the shapes the owner console renders — `SessionSummary`, `SessionDetail`, `ChatMessage`, and the `DashboardSnapshot`. `SessionDetail` returns curated view fields only (`summary`, `history`, `artifacts`); it does not leak raw `ThreadState`/`SessionState` records across the API — the SPA reads `summary.source`, and the raw `thread.json`/`session.json` remain available through the `Artifact viewer` rather than as response fields. The web route serves this facade's output verbatim. (Skill CRUD and audit-write helpers also currently live here; relocating them to the `skills`/`audit` slices is a known follow-up, not part of the read-facade contract.)
- `Usage read model`: the owner-facing token usage view owned by `src/slices/usage/index.ts`. It reads daily-partitioned usage records, validates and filters them by timezone-local windows, aggregates totals and breakdown rows, exposes the row-limit contract for the owner console, renders the markdown report used by the `usage-report` skill, and owns the dashboard's cached `tokensToday` counter. The web route, dashboard snapshot, and skill wrapper call this Module instead of rebuilding usage windows or report rows.
- `Event schema`: the single home for the thread event-kind union in `src/slices/events/index.ts`. The `type` frontmatter discriminant (`source_event`, `felix_reply`, `owner_permission`, `permission_request`) is defined once. `buildEventFile` produces a kind's on-disk shape (frontmatter, body, transcript lines, file-name slug); `parseEventFile` reads one back as a typed `ParsedEvent`; `toUniversalEvent`, `eventAt`, and `historyTitle` are the readers' shared decoders. `thread-store` owns only the IO (`writeThreadEventAt`/`writeThreadEvent`); the engine and owner console no longer hand-decode frontmatter, and no writer re-encodes a kind inline.
- `Source intake`: the source-neutral workflow that receives a source adapter's normalized source packet, persists raw source evidence, routes an `Owner decision` when the packet is from the `Owner`, and otherwise hands the event to the engine.
- `Source event normalization`: the source-neutral packet builder in `src/core/source-event-normalization.ts`. Source adapters still parse platform payloads and resolve source facts, then this Module constructs the `Thread key`, `source_thread_ref`, and `UniversalEvent` shape in one place. Adapter-owned helpers such as `mattermostThreadKey`, `slackSourceThreadRef`, and `whatsappThreadKey` delegate to it so the thread identity grammar has locality while preserving existing adapter interfaces.
- `Source host`: the source-neutral shell that runs a `Source driver`, in `src/core/source-host.ts`. It owns the lifecycle (`run(driver) → {stop, done}` plus the disabled-token no-op early return so a source without credentials starts as an inert stop/resolved pair), the platform-message dedup seen-cache (a bounded `Map` keyed by raw platform message id with a single-home TTL window, swept on insert so it cannot grow unbounded — this is the second dedup layer that catches platform redelivery *before* persistence, distinct from the engine's durable `hasThreadEvent`), and the attachment size-gate (`gateAttachment` throws the shared `AttachmentRejectedError`). All four adapters call `firstSight(id)` for dedup and `gateAttachment` for size limits; the host is the single home for the dedup window, eviction, and size-gate error grammar. Not to be confused with the `Agent runtime image` / `Runtime capability` (the container runtime) — "host" is the per-source shell, not the container. Reconnect/backoff stays adapter-owned until a second adapter needs it.
- `Source driver`: the platform-specific half a `Source host` runs — the adapter-supplied `{source, connect, disabled?}` where `connect(ingest)` opens the platform listener (WebSocket, Socket Mode, discord.js client, or wacli subprocess) and returns a `disconnect()`. The driver still parses payloads, posts replies, sends typing, and handles reactions; it calls the host's `firstSight`/`gateAttachment` inline where it already had the raw ids. The driver is what differs per platform; the host is what does not.
- `Mention grammar`: how a Mattermost bot name becomes an `@mention` token, owned once by `src/adapters/mattermost/mentions.ts` (`mattermostMentionToken(s)`, `normalizeMattermostName`). The matcher (`isMentioned`) and source-owned turn context (`getTurnContext`) both build tokens from it inside the Mattermost adapter, so the instruction Felix is given can never drift from what the adapter actually matches. The Mattermost adapter re-exports the token builders for external callers.
- `Harness`: the execution backend that runs a session. The `Harness` port is defined in `src/core/ports.ts`; `CodexHarness` in `src/adapters/codex/` is the sole adapter. The port takes intent — `TurnInput {thread, event, eventFile, contact, skills, resumed}` — and returns a `TurnResult` with `parsed: ParsedAgentOutput` and `success: boolean`. The engine holds a `Harness` field injected at construction; it never imports the Codex adapter directly. Adding a second harness (Claude, local model) means a new file in `adapters/`, no core edits. `FakeHarness` in `tests/helpers/` makes the engine loop unit-testable without spawning a process.
- `Project`: a checked-out target repository Felix can change on behalf of the Owner. The runtime stores projects under `$WORKSPACE_DIR/projects/<provider>/<namespace>/<repo>/`; provider is the git host family (`github`, `gitlab`), not the message source, namespace is the account or group, and repo is the repository name. Git adapters should treat this as the seam for branch, diff, PR/MR, and review work.
- `Project acquisition`: the `Software development skill` responsibility for locating an existing `Project` clone or cloning a missing repository into `$WORKSPACE_DIR/projects/<provider>/<namespace>/<repo>/` before code work begins; clone inputs must be an explicit HTTPS/SSH git URL or an exact `<provider>/<namespace>/<repo>` triple, and existing clones are inspected rather than automatically pulled.
- `Project-relative path`: a path reported relative to `$WORKSPACE_DIR`, such as `projects/github/acme/shop/docs/prds/change.md`; agent-facing replies prefer this form over host absolute paths.
- `Turn decision`: the pure dispatch logic for a single harness attempt, in `src/core/decide-turn.ts`. `decideTurnResult(result, resumed, retriedFreshStart) → TurnDecision` converts a `TurnResult` into one of: `retry_fresh`, `fail`, `reply`, `no_skill`, `permission_required`, `fallback`. No IO, no logging — the engine shell executes each case through ports. Tested directly in `tests/decide-turn.test.ts` without a running engine or temp dirs.
- `Turn runner`: the one-trigger execution workflow in `src/core/turn-runner.ts`: resolve source turn context, run the `Harness`, keep typing/status side effects alive during the attempt, handle retry-fresh and run-error loops, and delegate consequences to `Turn outcome`. The engine owns queue selection and thread locking; Turn runner owns the harness-attempt lifecycle.
- `Turn outcome`: the consequence executor for a classified harness attempt, in `src/core/turn-outcome.ts`. It calls `Turn decision`, then owns the side-effect choreography for retry-fresh, failed output, format correction, replies, permission requests, auto-grants, run-error requeueing, and usage/turn recording through engine-supplied ports. The engine still owns queue selection, thread locks, source adapters, and the initial harness run; tests hit the Turn outcome Module directly for consequence locality.
- `Turn cancellation`: the single owner of per-thread turn stopping, in `src/core/turn-cancellation.ts`. It concentrates the request-flag set and the in-flight `AbortController` map that previously lived as raw engine fields, behind named operations: `request` (flag + abort the active signal), `isRequested`, `clear`, `begin` (install a fresh controller, return its `AbortSignal`), and `end` (drop the controller, clear the flag). The engine holds one instance and exposes `isRequested`/`clear` to the `Turn runner` and `Turn outcome` through their existing ports, so those Modules stay unaware of the controller wiring. The request→check→clear cycle has one home instead of being traced across three files, and cancellation semantics are unit-tested without a running engine.
- `Event routing`: the pure accept/filter predicates in `src/core/routing.ts`. `shouldAcceptEvent(event)` — DMs always accepted, channel posts require `mentions_bot`. `isOwnMattermostMessage(event, botUserId)` — filters the bot's own posts from the processing queue. Both are thin-wrapped by the engine's private methods; tested directly in `tests/routing.test.ts`.
- `Skill`: an installed capability defined by a `SKILL.md` file on disk.
- `Skill ID`: the directory name under `workspace/.agents/skills`; frontmatter `name` mirrors it per the agentskills.io standard.
- `Software development skill`: the bundled operational `Skill` that adapts Matt Pocock's engineering workflow for Felix's skill format and permission model; it is not a vendored mirror of upstream skill files, read-only asking is open to all users, and mutations are gated by `repo.write`.
- `Promoted Matt skill`: a Matt Pocock skill from the upstream `engineering` or `productivity` buckets that is stable enough to adapt into Felix workflows; excludes upstream `deprecated`, `misc`, `personal`, and `in-progress` buckets.
- `Local work artifact`: a Markdown file produced by the `Software development skill` inside the target `Project` worktree under `docs/prds/`, `docs/issues/`, or `docs/handoffs/`; these folders are created lazily and replace external issue tracker publishing and the current `task-manager` workflow for this skill.
- `Agent runtime image`: the container runtime Felix uses to execute harness turns and skills, intentionally carrying common language and utility tooling for predictable session work while leaving provider-specific operational CLIs to explicit installation or skill setup.
- `Runtime capability`: a stable category of work the Agent runtime image guarantees for skills, such as Node execution, Python execution, data analysis, chart generation, document/PDF utility work, shell/network utilities, or Git/project editing.
- `Runtime package`: a concrete installed package or executable that currently implements a `Runtime capability`; package lists are operational details and may change without redefining the capability.
- `Shared runtime tooling`: owner- or skill-installed executables and user-space language packages stored under `workspace/runtime/` so they are available to later Felix sessions without mutating bundled skills, session records, or the base image filesystem.
- `Core data stack`: the provider-neutral Python data and visualization packages available in the agent runtime image for common reporting and artifact generation, with dependency expansion handled as shared runtime tooling rather than per-session virtual environments.
- `Document conversion suite`: heavy office-document conversion tooling such as LibreOffice; excluded from the initial Agent runtime image because the cost is large relative to the current guaranteed runtime capabilities.
- `Browser automation runtime`: browser binaries and their system dependencies for Playwright or similar browser automation; excluded from the initial Agent runtime image until bundled skills require in-container browser execution.
- `Source API posting`: direct agent use of the current source platform API to post intermediate messages or generated session artifacts in the active thread before the final reply. Mattermost is the current concrete source; future sources such as Slack must provide their own source-specific posting instructions.
- `Monthly cost summary report`: an AWS Jala reporting use case that summarizes the latest completed month and compares it with the previous completed month.
- `Report artifact bundle`: the files produced by a reporting use case as its primary output, usually including source data, visualizations, and a written summary.
- `Session view`: inspect-only in the first version; no manual rerun control.
- `Skill editor`: can edit name, description, permissions, and body.
- `Landing page`: simple list of sessions grouped by thread.
- `Session detail`: shows session history and raw artifacts; raw logs are behind the detail view. The `/api/sessions/{key}` response is the single source of truth — it carries `artifacts` inline, so there is no separate `/artifacts` endpoint for the UI to call.
- `UI data access`: internal JSON endpoints served by the existing app.
- `Contact editor`: the Owner UI use case for creating or editing one per-source contact record. Its Module is `src/slices/contacts/index.ts`: `listContacts`, `loadContactForEditor`, `createContactFromEditor`, and `updateContactFromEditor` own contact editor normalization, existence checks, and persistence. Routes translate HTTP status codes and audit entries; they do not assemble `ContactRecord` objects.
- `Approval registry`: the authoritative store of approval records (`src/slices/approvals/registry.ts`, public face `src/slices/approvals/index.ts`), written by the engine at request- and decision-time. It is the pending query Module for approvals: `listPendingApprovals`, `findPendingApproval`, and `hasPendingApproval` read live pending records, match an `Owner decision` by thread key, approval id, or owner-message anchor, and resolve the owning `Session` only after the registry record matches. The slice also owns the compatibility pending-thread resolution aliases (`listPendingPermissionThreads`, `resolvePendingPermissionThread`, `resolvePendingPermissionThreadExact`) as named queries in `registry.ts` itself, alongside the owner-decision grammar (`decision.ts`) and decision orchestration (`apply.ts`). The single source of truth for approvals — the UI and owner console read from it; nothing re-derives approvals from event files or scans Sessions to discover live requests.
- `Approval request lifecycle`: the end-to-end workflow for a permission request after a harness turn emits `PERMISSION_REQUIRED`: build the durable request, notify the `Owner`, store the pending approval through the `Approval registry`, apply an `Owner decision`, update the Owner-facing message when possible, post the decision notification back to the source thread, and queue the proceed event when work should continue. This Module should keep those side effects behind one interface so the engine does not know the approval ordering.
- `Approval requests`: visible in the UI with history of requested, approved, and rejected permissions.
- `Approval action`: the Owner can approve or reject approval requests from the UI.
- `Permission grant`: the additive contact authorization an `always` decision produces — one skill plus a set of permissions, de-duplicated against what the contact already holds. `decideApproval` names the grant intent (`PermissionGrant` in `src/approvals.ts`); `grantPermissions` in `src/slices/contacts/index.ts` is the single home for the merge-and-persist. Nothing else merges contact grants.
- `Owner decision`: a typed owner verdict on a pending request (`OwnerDecision` in `src/types.ts`) — a `mode` (`once`/`always`/`reject`), the decider, and a `target` locating the pending thread (by `threadKey` from the UI, or by source-neutral owner-message anchor from an adapter). Transport-independent: adapters build it; the engine replays it. The reply grammar lives in `src/slices/approvals/decision.ts`, not in any adapter — as does the owner-console action grammar (`ownerDecisionFromAction(action, scope)` maps a REST `approve`/`reject` + scope to a decision mode), so the web route builds a decision through the slice instead of hand-rolling the mapping. Applying a decision is `applyOwnerDecision` in `src/slices/approvals/apply.ts`: it resolves the target thread, records the verdict (`decideApproval`), and on an `always` decision persists the `Permission grant` (`grantPermissions`) — the one place that turns grant intent into a stored grant, so `approvals` stays decoupled from `contacts`. The engine only queues the decision event and runs the turn.
- `Owner decision routing`: the source-neutral intake step that turns an Owner's platform-specific reply or reaction into an `Owner decision` target, only when it locates a matching pending approval.
- `Tracked owner-decision dispatch`: the WhatsApp-only realization of `Owner decision routing`. WhatsApp has no native link from a reply/reaction back to the bot's original permission message, so the adapter persists tracked bot messages on disk (`addTrackedBotMessage`/`getTrackedBotMessage`/`removeTrackedBotMessage` in `src/adapters/whatsapp/index.ts`) and resolves an `Owner decision` against them. A reply or a reaction can arrive either FromMe (owner shares the bot's number) or from a separate owner number; `dispatchTrackedOwnerReply` and `dispatchTrackedOwnerReaction` are the single home for "tracked bot message → `Owner decision routing` → clear tracking", so the four webhook call-sites cannot drift. This stays inside the WhatsApp adapter rather than `source-intake.ts` because message tracking is WhatsApp-specific — other sources rely on native reply/reaction linkage, so promoting it to core would be a one-source seam.
- `Artifact viewer`: raw artifacts are shown in read-only markdown/text views.
- `Navigation`: top-level tabs for `Sessions`, `Skills`, `Contacts`, and `Approvals`.
- `Data fetching`: React `useQuery`-style client fetching with automatic refresh.
- `Auth`: owner login with an httpOnly session cookie.
- `Audit view`: simple history panel for owner edits and approval actions.
- `Exposure`: internal-only operator UI, not a public surface.
- `Database connection`: a named configuration for connecting to a database engine, stored as an encrypted JSON file under `workspace/databases/connections/<alias>.json`. Contains engine-specific configuration (host, port, user, database), encrypted credentials, optional SSH tunnel config, and SSL settings.
- `Database engine`: the database system type — PostgreSQL, MySQL/MariaDB, SQLite, MongoDB, Redis, DynamoDB, or Cosmos DB. Each engine has its own connection parameters and Node.js driver.
- `Connection alias`: the human-readable name used to reference a database connection in commands and permissions (e.g., `prod-pg`, `staging-mysql`).
- `Permission tier`: the access level granted for a database connection — `read` (SELECT, introspection), `write` (INSERT, UPDATE, DELETE, DDL), or `admin` (user management, grants, backups). Configurable per connection.
- `Two-phase permission`: historical — the database-skill prose convention where base permissions were server-computed and connection-specific permissions were LLM-resolved. Superseded by first-class `Scoped permission` matching; the server now reports scoped grants and only concrete scope selection stays with the LLM.
- `Scoped permission`: a permission carrying a scope segment — `<name>.<scope>` (e.g. `database:read.prod-pg`). A permission is scoped only when the skill declares it as `<name>.*`; because names may themselves contain dots, the declaration — not the string shape — defines where the name ends and the scope begins. A scoped grant satisfies only its exact scope; a `Wildcard grant` satisfies every scope of that name. A bare (unscoped) permission and a scoped permission of the same name are distinct — neither satisfies the other. Matching is owned by `src/slices/skills/index.ts`.
- `Scope`: the variable segment after the permission name identifying the concrete resource a grant applies to (e.g. a `Connection alias`). Opt-in per permission — most skills use only unscoped permissions.
- `Wildcard grant`: a contact grant of the form `<name>.*`, satisfying any scope of that permission name. Full wildcard only — no partial patterns (`read.staging-*`).
- `DB_ENCRYPTION_KEY`: a 32-byte random key auto-generated during setup, stored in `.env`, used to encrypt/decrypt database connection credentials at rest.
- `Release`: a customer-distributable Felix runtime image accompanied by its version, release notes, and operating instructions.
- `Release version`: the semantic version assigned to a Release; `0.1.0` is the first stable Release version. A leading `v` may be used only as the Git tag prefix.
- `Supported integration`: a source or harness included in the `0.1.0` core runtime support promise; it must have customer-facing configuration guidance and an acceptance test.
- `Support matrix`: the explicit list of Supported integrations, bundled-skill capabilities, architectures, and deployment environments covered by a Release.
- `Supported deployment environment`: the environment covered by the Release support promise; for `0.1.0`, this is Docker Compose running on a single host.
- `Workspace data`: the customer-owned persistent state under the Felix workspace, including sessions, approvals, audit history, skills, credentials, and runtime state.
- `Internal persistence`: Felix's own filesystem-based storage under the Workspace data directory; Felix does not require or run on an internal database engine.
- `Skill capability`: a customer-usable behavior packaged as a Skill; database engine support belongs to the `database` Skill rather than to Felix's core runtime integration list.
- `Bundled skill`: a Skill shipped inside the `0.1.0` image and included in the Release promise; it is not a separately distributed product.
- `Bundled skill acceptance`: the Release evidence that each Bundled skill loads, declares its prerequisites, and completes its basic smoke test; customer-owned external services and credentials remain prerequisites rather than image contents.
- `Prerequisite integration test`: an acceptance test that exercises a Bundled skill against a customer-owned external service or credential when that prerequisite is available.
- `Release package`: the customer-facing set shipped with a Release: the versioned image, GitHub release metadata, operating documentation, security and legal notices, image evidence, and rollback materials.
- `Data durability policy`: the Release promise that Workspace data survives supported upgrades and can be recovered using the documented backup and restore procedure.
- `Network exposure policy`: the Release rule for reachability; the Owner console is private by default, while any public webhook access requires customer-managed HTTPS, firewall controls, and webhook authentication.
- `Update policy`: customers upgrade manually to a versioned image or immutable digest after backing up Workspace data; the `latest` alias is not a production deployment target.
- `Release gate`: the set of checks that must pass before a Release is customer-distributable; for `0.1.0`, failures in supported integrations, security checks, image validation, upgrade, backup, or restore block publication.
- `Release evidence`: the recorded results demonstrating that a Release passed its Release gate, including software checks, image checks, integration acceptance, lifecycle tests, and customer documentation review.
- `Security exception`: a documented, time-bounded decision to ship with a non-blocking vulnerability, including its risk, affected dependency or path, mitigation, owner, and review date.
- `Support policy`: the customer assistance promise for a Release; `0.1.0` provides best-effort issue-tracker support without uptime, response-time, or resolution-time guarantees.
- `Data-handling policy`: Felix collects no Felix-owned telemetry; Workspace data remains on the customer's host, while messages and prompts may be sent to the customer-selected source and model providers.
- `License policy`: `0.1.0` is distributed under Apache-2.0, with applicable third-party notices included.
- `Distribution policy`: the official source is the public GitHub repository and the official customer image is the matching public Docker Hub repository; customers use the versioned image tag and GitHub release notes.
